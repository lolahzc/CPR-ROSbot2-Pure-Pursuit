# CPR-ROSbot2-Pure-Pursuit
Para hacer pruebas con el nuevo paquete solo hay que lanzar el simulador *(readme del main)** y ejecutar el siguiente comando:

``
ros2 launch pure_pursuit_controller pure_pursuit.launch.py  
`
